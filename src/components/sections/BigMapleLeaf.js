"use client";

// Retired: maple-leaf decoration removed per new theme (dark/light .env-driven).
// Keeping stub to avoid breaking stray imports — renders nothing.
export default function BigMapleLeaf() {
  return null;
}
